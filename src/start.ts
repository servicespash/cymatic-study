// import { createStart, createMiddleware } from "@tanstack/react-start";
import { renderErrorPage } from "./lib/error-page";

// Client-side fallback architecture safely preserved without server execution crashes
export const errorMiddleware = null;
export const startInstance = null;

console.log("SPA Client Route Environment Active: Server-side handlers deferred.");
